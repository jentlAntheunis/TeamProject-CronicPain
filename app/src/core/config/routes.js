const AuthRoutes = {
  Index: '/auth',
  Login: '/auth/login',
  Register: '/auth/register',
}

const PatientRoutes = {
  Dashboard: '/',
  Progress: '/progress',
  Shop: '/shop',
}

export { AuthRoutes, PatientRoutes };
